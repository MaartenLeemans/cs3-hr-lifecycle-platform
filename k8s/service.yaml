apiVersion: v1
kind: Service
metadata:
  name: hr-service
  namespace: hr-platform
spec:
  type: NodePort
  selector:
    app: hr-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30080
